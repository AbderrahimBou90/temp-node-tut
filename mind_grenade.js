const num1 = 5 
const num2 = 10 

function addValue() {
 console.log(`the sum is: ${num1 + num2}`)
}

addValue()

// if we have a function inside of the module that we invok yes this code will run even though we didn't assign it to a variable as you can see in 3-modules.js file 